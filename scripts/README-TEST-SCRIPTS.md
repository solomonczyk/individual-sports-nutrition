# Скрипты быстрого запуска для тестирования

Быстрые скрипты для запуска мобильной и веб версий приложения.

## 📱 Мобильная версия

### Windows (PowerShell)
```powershell
# Интерактивный запуск (выбор платформы в меню)
.\scripts\test-mobile.ps1

# Прямой запуск для Android
.\scripts\test-mobile.ps1 android

# Прямой запуск для iOS
.\scripts\test-mobile.ps1 ios
```

### Linux/Mac (Bash)
```bash
# Интерактивный запуск (выбор платформы в меню)
./scripts/test-mobile.sh

# Прямой запуск для Android
./scripts/test-mobile.sh android

# Прямой запуск для iOS
./scripts/test-mobile.sh ios
```

## 🌐 Веб-версия

### Windows (PowerShell)
```powershell
.\scripts\test-web.ps1
```

### Linux/Mac (Bash)
```bash
./scripts/test-web.sh
```

Веб-версия будет доступна на `http://localhost:8081` (или другой порт, если 8081 занят).

## ⚙️ Что делают скрипты

1. ✅ Проверяют наличие Node.js и npm
2. ✅ Переходят в директорию `mobile-app`
3. ✅ Устанавливают зависимости (если `node_modules` отсутствует)
4. ✅ Запускают соответствующую версию приложения

## 📝 Примечания

- **Мобильная версия**: Использует Expo. После запуска можно выбрать платформу в интерактивном меню или указать напрямую.
- **Веб-версия**: Запускает Expo в веб-режиме.
- Скрипты автоматически устанавливают зависимости при первом запуске.
- Убедись, что Backend API запущен на `http://localhost:3000` для корректной работы приложения.

## 🚀 Быстрый старт

1. Запусти Backend API (если еще не запущен):
   ```bash
   cd backend-api
   npm run dev
   ```

2. Запусти мобильную или веб версию:
   ```powershell
   # Windows
   .\scripts\test-mobile.ps1
   # или
   .\scripts\test-web.ps1
   ```

   ```bash
   # Linux/Mac
   ./scripts/test-mobile.sh
   # или
   ./scripts/test-web.sh
   ```

