# Быстрая инструкция: Настройка SSH доступа без пароля

## Ваш публичный SSH ключ:

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILHikeZvz93JntBKCdfVTqkyU1U5uzBT3+Wh6uk5FcW7 tarasenko04@gmail.com@github.com
```

## Способ 1: Автоматический (рекомендуется)

Запустите скрипт:

```powershell
.\scripts\setup-ssh-access.ps1
```

Скрипт попросит пароль один раз, затем установит ключ и всё настроит.

## Способ 2: Вручную (если скрипт не работает)

### Шаг 1: Подключитесь к серверу с паролем (один раз)

```bash
ssh root@152.53.227.37
```

### Шаг 2: На сервере выполните команды:

```bash
mkdir -p ~/.ssh
chmod 700 ~/.ssh
nano ~/.ssh/authorized_keys
```

### Шаг 3: Вставьте ваш публичный ключ в файл

Скопируйте и вставьте эту строку в открывшийся редактор:

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILHikeZvz93JntBKCdfVTqkyU1U5uzBT3+Wh6uk5FcW7 tarasenko04@gmail.com@github.com
```

Сохраните (Ctrl+O, Enter, Ctrl+X в nano).

### Шаг 4: Установите правильные права

```bash
chmod 600 ~/.ssh/authorized_keys
exit
```

### Шаг 5: Проверьте подключение без пароля

```bash
ssh root@152.53.227.37
```

Если всё настроено правильно, подключение произойдёт БЕЗ запроса пароля! 🎉

## Способ 3: Одна команда PowerShell

Если у вас уже настроен доступ по паролю, выполните:

```powershell
$pubKey = Get-Content $env:USERPROFILE\.ssh\id_ed25519.pub
ssh root@152.53.227.37 "mkdir -p ~/.ssh && chmod 700 ~/.ssh && echo '$pubKey' >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys"
```

Введите пароль один раз, и всё готово!

